{% extends 'base.html.twig' %}

{% block title %}CommonParam{% endblock %}

{% block body %}
    <h1>CommonParam</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ common_param.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ common_param.name }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ common_param.value }}</td>
            </tr>
            <tr>
                <th>Created_at</th>
                <td>{{ common_param.createdAt ? common_param.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Updated_at</th>
                <td>{{ common_param.updatedAt ? common_param.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('common_common_param_index') }}">back to list</a>

    <a href="{{ path('common_common_param_edit', {'id': common_param.id}) }}">edit</a>

    {{ include('common_common_param/_delete_form.html.twig') }}
{% endblock %}
